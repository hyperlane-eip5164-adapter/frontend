import { Box, Heading } from "@chakra-ui/react";

const Footer = () => {
  return <Box as={'footer'} p={6} bg={'black'}>

    <Heading as={'h4'} size={'lg'} color={'brand'}>Pontis</Heading>
    <Box>

    </Box>
  </Box>;
};

export default Footer;
